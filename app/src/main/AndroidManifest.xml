<?xml version="1.0" encoding="utf-8"?>
<!--
  Minimal manifest policy: one launcher Activity, no feature Activities/Services/Receivers.
  Navigation, permissions, and feature wiring live in Kotlin (Compose + runtime APIs).
  See docs/ARCHITECTURE.md § "Minimal manifest".
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".OmsApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.OMS">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.OMS">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
